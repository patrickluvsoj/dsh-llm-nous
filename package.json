{
  "name": "dsh-llm-nous",
  "version": "0.1.0",
  "description": "Nous Portal OpenAI-compatible LLM adapter bundle for DeepSeek Harness",
  "type": "module",
  "main": "lib/index.mjs",
  "files": ["lib", "cordis.patch.yml", "README.md", "docs", "package.json"],
  "scripts": {
    "build": "tsdown",
    "test": "node --import tsx/esm --test tests/*.test.ts"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsdown": "^0.22.2",
    "tsx": "^4.22.0",
    "typescript": "^6.0.0"
  }
}
